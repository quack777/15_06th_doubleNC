import React from 'react';
import styled from 'styled-components';
interface Props {
  count: string;
}
const NumberOfItems = () => {
  <Container>00개의 상품</Container>;
};

const Container = styled.div`
  width: 100%;
`;
export default NumberOfItems;
